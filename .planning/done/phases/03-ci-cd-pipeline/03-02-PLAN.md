---
phase: 03-ci-cd-pipeline
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .github/workflows/deploy.yml
  - .github/README-secrets.md
autonomous: false
requirements:
  - BG-06
  - BG-07
  - BG-08
  - BG-09
must_haves:
  truths:
    - "GitHub repository has all required secrets configured (EC2_SSH_KEY, EC2_HOST, DOCKER_USERNAME, DOCKER_PASSWORD)"
    - "Pipeline runs end-to-end on a real commit to main and succeeds"
    - "Immutable SHA tag is visible in Docker Hub after a successful pipeline run"
    - "Smoke test (GET/POST/PUT/DELETE /todos) passes against the public IP"
    - "Lock file behavior is verified: acquired before deploy, released on success"
  artifacts:
    - path: ".github/README-secrets.md"
      provides: "Step-by-step guide for configuring all required GitHub Actions secrets"
      min_lines: 30
  key_links:
    - from: "GitHub Actions"
      to: "EC2 (13.236.205.122)"
      via: "EC2_SSH_KEY secret + appleboy/ssh-action"
      pattern: "secrets\\.EC2_SSH_KEY"
    - from: "GitHub Actions"
      to: "Docker Hub"
      via: "DOCKER_USERNAME + DOCKER_PASSWORD secrets + docker/login-action"
      pattern: "secrets\\.DOCKER"
---

<objective>
Configure GitHub Actions secrets and run an end-to-end pipeline test. This validates that the CI/CD pipeline works in practice: secrets are set, the pipeline triggers, the Docker image is pushed with an immutable SHA tag, deployment succeeds on EC2, and the smoke test passes.
</objective>

<context>
@.github/workflows/deploy.yml (created in 03-01 — contains the pipeline definition)
@.planning/phases/03-ci-cd-pipeline/03-RESEARCH.md (secrets management section)
@.planning/ROADMAP.md (Phase 3 success criteria: full pipeline tested end-to-end)
@scripts/ec2-lock.sh (created in 03-01 — verify lock behavior)

**Locked decisions (non-negotiable):**
- Docker Hub image: `mythicc123/multi-container-service`
- SSH host: `ubuntu@13.236.205.122`
- Public IP for smoke test: `13.236.205.122`
- API endpoints: GET/POST/PUT/DELETE on `/todos` path (via Nginx routing to active blue-green slot)

**Secrets required:**
- `EC2_SSH_KEY`: Full private key content (multi-line)
- `EC2_HOST`: `13.236.205.122`
- `DOCKER_USERNAME`: Docker Hub username
- `DOCKER_PASSWORD`: Docker Hub password or access token
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Configure GitHub Actions secrets</name>
  <what-built>README-secrets.md documenting the 4 required secrets and step-by-step configuration instructions</what-built>
  <how-to-verify>
Create `.github/README-secrets.md` with step-by-step instructions:

1. **Navigate to Secrets:** Go to GitHub repo -> Settings -> Secrets and variables -> Actions
2. **Create each secret:**

   **EC2_SSH_KEY:**
   - Name: `EC2_SSH_KEY`
   - Value: Paste the FULL private key from `C:\Users\fiefi\.ssh\ec2-static-site-key.pem`
   - Include all lines: `-----BEGIN OPENSSH PRIVATE KEY-----` through `-----END OPENSSH PRIVATE KEY-----`
   - The key must be unencrypted (no passphrase)

   **EC2_HOST:**
   - Name: `EC2_HOST`
   - Value: `13.236.205.122`

   **DOCKER_USERNAME:**
   - Name: `DOCKER_USERNAME`
   - Value: `mythicc123`

   **DOCKER_PASSWORD:**
   - Name: `DOCKER_PASSWORD`
   - Value: Docker Hub password or access token
   - Recommendation: Use an access token (Settings -> Security -> Access Tokens) rather than password, with push permission

3. **Verify all 4 secrets are listed** in the GitHub Actions Secrets page.

4. **Verify SSH key works:** The workflow will attempt SSH connection immediately. Ensure the key is unencrypted and the public key is in `~/.ssh/authorized_keys` on the EC2 instance.

**Content for `.github/README-secrets.md`:**

```markdown
# GitHub Actions Secrets Configuration

This document describes the secrets required for the blue-green deployment pipeline.

## Required Secrets

All secrets are configured at: **Settings -> Secrets and variables -> Actions -> New repository secret**

| Secret Name | Value | Where to Get |
|------------|-------|--------------|
| `EC2_SSH_KEY` | Full private key PEM content | `C:\Users\fiefi\.ssh\ec2-static-site-key.pem` |
| `EC2_HOST` | `13.236.205.122` | Fixed EC2 public IP |
| `DOCKER_USERNAME` | Docker Hub username | `mythicc123` |
| `DOCKER_PASSWORD` | Docker Hub password or access token | Docker Hub account settings |

## EC2_SSH_KEY Setup

1. Read the private key:
   ```bash
   cat ~/.ssh/ec2-static-site-key.pem
   ```
2. Copy the entire output including `-----BEGIN OPENSSH PRIVATE KEY-----` and `-----END OPENSSH PRIVATE KEY-----`
3. Paste into the `EC2_SSH_KEY` secret value field in GitHub
4. **IMPORTANT:** The key must be unencrypted (no passphrase). If the key has a passphrase, remove it:
   ```bash
   ssh-keygen -p -f ~/.ssh/ec2-static-site-key.pem
   # Enter old passphrase, leave new passphrase empty
   ```

## DOCKER_PASSWORD Setup

Using an access token instead of password is recommended:

1. Go to Docker Hub -> Account Settings -> Security -> Access Tokens
2. Create new token with:
   - Token name: `github-actions-deploy`
   - Access permission: `Read, Write, Delete`
3. Copy the generated token as the `DOCKER_PASSWORD` secret value

## Verification

After setting secrets, push a commit to `main` and verify:
1. The workflow starts in the Actions tab
2. The Docker build step succeeds
3. The deploy step completes (look for "LOCK ACQUIRED" and "LOCK RELEASED" in logs)
4. The smoke test step passes (look for "SMOKE TEST: ALL PASSED")
5. The workflow shows a green checkmark

## Troubleshooting

**SSH connection refused:**
- Verify EC2 is running and accessible from the internet
- Check the public IP is correct
- Verify the public key is in `~/.ssh/authorized_keys` on EC2

**Docker push failed:**
- Verify `DOCKER_USERNAME` and `DOCKER_PASSWORD` are correct
- Ensure the Docker Hub account has push access to `mythicc123/multi-container-service`

**Deploy step failed:**
- Check that Phase 2 scripts exist at `scripts/*.sh`
- Verify `/tmp/deploy/` scripts were installed by the setup step
- Check EC2 lock file: `ssh ubuntu@13.236.205.122 "cat /tmp/blue-green-deploy.lock"`
```

**After creating the file:**
1. Verify the file exists: `test -f .github/README-secrets.md`
2. Verify it documents all 4 secrets
3. Then configure the secrets manually via the GitHub web UI
</how-to-verify>
  <resume-signal>Type "approved" or describe issues after configuring secrets</resume-signal>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Run end-to-end pipeline test</name>
  <what-built>Real GitHub Actions pipeline run triggered by pushing a test commit to main</what-built>
  <how-to-verify>
1. **Push a test commit to main:**
   ```bash
   git checkout main
   git pull origin main
   # Make a trivial change (e.g., add a comment to app/Dockerfile)
   git add -A
   git commit -m "test: trigger CI/CD pipeline"
   git push origin main
   ```

2. **Monitor the workflow run:**
   - Go to GitHub repo -> Actions tab
   - Wait for "Deploy to EC2 (Blue-Green)" workflow to start
   - Watch the logs for each step

3. **Expected step sequence:**
   - Checkout code (green)
   - Set up Docker Buildx (green)
   - Login to Docker Hub (green)
   - Build and push Docker image (green) — look for `mythicc123/multi-container-service:sha-<sha>`
   - Install deploy scripts on EC2 (green) — look for "Installed: /tmp/deploy/"
   - Run blue-green deployment (green) — look for "LOCK ACQUIRED", deploy output, "LOCK RELEASED"
   - Smoke test API endpoints (green) — look for "SMOKE TEST: ALL PASSED"

4. **Verification criteria:**
   - All steps pass (green checkmarks)
   - Docker Hub shows new image tag with SHA (verify at hub.docker.com)
   - Smoke test shows ALL PASSED
   - Lock was acquired and released (not stale)
   - EC2 state file updated: `ssh ubuntu@13.236.205.122 "cat /var/run/blue-green-state"`

5. **Manual smoke test (optional but recommended):**
   ```bash
   # Test from local machine after pipeline completes
   curl -s http://13.236.205.122/health
   curl -s http://13.236.205.122/todos
   ```
</how-to-verify>
  <resume-signal>Type "approved" or describe failures. If workflow failed, diagnose and fix before approval.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify immutable SHA tagging on Docker Hub</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
After the pipeline succeeds, verify that Docker Hub received an immutable SHA-tagged image.

**Verification steps (run from local machine):**

1. **Check Docker Hub for the SHA tag:**
   ```bash
   # Authenticate to Docker Hub
   docker login -u mythicc123

   # Pull the SHA-tagged image (replace <sha> with actual GitHub SHA from the workflow run)
   docker pull mythicc123/multi-container-service:sha-<GITHUB_SHA>

   # Verify it exists and is the correct image
   docker images mythicc123/multi-container-service
   ```

2. **Confirm no `latest` tag was pushed by the deploy pipeline:**
   - In the GitHub Actions logs, search for "tags:" in the build-push-action step
   - The only tag should be `mythicc123/multi-container-service:sha-<sha>`

3. **Document the immutable tag in a verification comment:**
   Add a comment to the GitHub Actions workflow file noting the immutable tag pattern:
   ```yaml
   # NOTE: Only push SHA-tagged images in the deploy pipeline.
   # The 'latest' tag is NEVER pushed by CI/CD.
   # To update 'latest', run manually: docker push mythicc123/multi-container-service:latest
   ```

**Why this matters:** Each production deployment is tied to a specific GitHub commit SHA. Rolling back to a previous version is just a matter of re-running the pipeline with that SHA (or manually pulling the image). There is no ambiguity about which code is running.
</action>
  <verify>
    <automated>
      # Verify the SHA tag was pushed to Docker Hub
      # Run after pipeline completes — replace <GITHUB_SHA> with actual value
      docker pull mythicc123/multi-container-service:sha-<GITHUB_SHA> 2>/dev/null && echo "SHA tag verified on Docker Hub" || echo "SHA tag NOT found"
    </automated>
  </verify>
  <done>Docker Hub shows the immutable SHA-tagged image, deploy pipeline is fully validated end-to-end</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. GitHub Actions workflow runs successfully on a real commit
2. Docker Hub contains the SHA-tagged image
3. Smoke test passed (GET/POST/PUT/DELETE /todos)
4. Lock was properly acquired and released
5. `/var/run/blue-green-state` on EC2 reflects the active slot
</verification>

<success_criteria>
- All 4 GitHub Actions secrets configured (EC2_SSH_KEY, EC2_HOST, DOCKER_USERNAME, DOCKER_PASSWORD)
- `.github/README-secrets.md` documents the secret setup process
- Pipeline runs end-to-end on a real commit to main
- Immutable SHA tag pushed to Docker Hub (no `latest` from CI/CD)
- Smoke test passes against public IP (GET/POST/PUT/DELETE)
- Lock acquisition and release verified in workflow logs
- EC2 state file correctly updated
</success_criteria>

<output>
After completion, create `.planning/phases/03-ci-cd-pipeline/03-02-SUMMARY.md`
</output>
